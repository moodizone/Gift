import { Metadata } from "next";
import * as React from "react";

export const metadata: Metadata = {
  title: "Products",
};

function page() {
  return <div>product goes here</div>;
}

export default page;
